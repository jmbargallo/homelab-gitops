apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: local-path-provisioner-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:jmbargallo/homelab-gitops.git
    targetRevision: main
    path: clusters/mhga/apps/local-path-provisioner/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: local-path-storage
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
